import { Button, Label, Col, FormGroup } from "bootstrap";
import { Formik,Field, Form, ErrorMessage } from "formik";

const RegistrationForm = () => {

    return <Formik>
        <Form>
            
        </Form>
    </Formik>
};

export default RegistrationForm;